'use client';

import React from 'react';

interface NavigationHeaderProps {
  show: boolean;
}

// Logo/wordmark intentionally hidden — placeholder removed.
// Re-enable by replacing `return null` with the conditional render below.
export function NavigationHeader({ show: _show }: NavigationHeaderProps) {
  return null;
}
